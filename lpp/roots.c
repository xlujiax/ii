#include "roots.h"

int bezier_quad_roots(Bezier* b, float** roots)
{
  assert(b);
  assert(b->n == 2);

  // quadratic polynomial coefficients in power basis
  //float a = c->;
  //float b = ;
  //float c = ;
  
  return 0;
}
